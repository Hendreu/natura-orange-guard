# Postgres container credentials
POSTGRES_DB=qualys_db
POSTGRES_USER=qualys_vis
POSTGRES_PASSWORD=change_me

# App DATABASE_URL — "postgres" is the Docker compose service name, NOT localhost
DATABASE_URL=postgres://qualys_vis:change_me@postgres:5432/qualys_db

# Runtime
PORT=3000
HOST=0.0.0.0